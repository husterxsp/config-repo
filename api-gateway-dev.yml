zuul:
  routes:
    # 简写
#    product: /myProduct/**
     user: /user/**
     myProduct:
       path: /myProduct/**
       serviceId: product
  sensitiveHeaders:
  host:
    socket-timeout-millis: 60000
    connect-timeout-millis: 60000

  add-host-header: true
